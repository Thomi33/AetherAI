# Dependencias SOLO de la API. El motor (LangGraph, ollama, textual, etc.)
# se instala con ../requirements.txt (venv del proyecto, ver run.sh).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8
pydantic-settings>=2.4
requests>=2.31
